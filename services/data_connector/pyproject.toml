[project]
name = "data-connector"
version = "0.1.0"
description = "AIAL Oracle data connector service"
requires-python = ">=3.12"
dependencies = [
    "oracledb>=3.4.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["."]

